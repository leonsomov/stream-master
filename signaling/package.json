{
  "name": "stream-master-broker",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket signaling broker for Stream Master. Pairs sender/receiver by stream ID and relays WebRTC SDP + ICE candidates.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
